# 强化学习学习路径

这份文档面向**系统学习**：不只是按顺序跑通环境，而是每个环境都配一套「跑通 → 对照 → 诊断」的实验，用来把抽象概念钉在具体的数字上。

项目里有 13 个配置，覆盖从离散控制到像素输入、从稠密奖励到稀疏奖励的谱系。但**它覆盖不了 RL 的全部**——最后一节列出了边界，那是你学完这里之后该去的方向。

## 怎么用这份文档

### 三步法

对每个环境，按这三步走，缺一步都不算学会：

1. **跑通**（基线）—— 用默认配置训练，知道成功长什么样
2. **对照**（扰动一个变量）—— 只改一个参数/算法重跑，看差异
3. **诊断**（解释差异）—— 用日志里的具体数字解释「为什么」

第 3 步是分水岭。能跑通环境的人很多，能说出「这里回报卡住是因为 critic 没信号」的人才算入门。

### 实验记录规范

每个实验记录四样东西，缺一不可：

| 项 | 例子 |
| --- | --- |
| 自变量 | `learning_rate: 1e-3`（其他全默认） |
| 因变量 | `evaluation.json` 的 `mean_reward` 与收敛步数 |
| 结果 | 具体数字，不是「变好了」 |
| 解释 | 机制层面的原因 |

**不要写「效果好」「效果差」**。写「1e-3 在 8k 步达到 500，3e-4 在 20k 步达到 500，1e-4 在 50k 步只到 380 —— 学习率与收敛速度反相关，但 1e-3 的评估方差是 3e-4 的 2 倍」。

### 关于随机性

RL 的实验**高度依赖种子**。只跑一次得到的结论基本没有意义。规则：

- 关键结论至少跑 3 个种子（改 `experiment.seed`）
- 比较两个配置时，用相同的种子集合
- 报告时给出均值和范围，不是单次值

这一点在阶段 0 有专门的实验让你亲身体会。

---

## 知识地图

### 这个项目能覆盖的

| 概念 | 对应环境 | 阶段 |
| --- | --- | --- |
| MDP、回合、回报、折扣 | 全部 | 0 |
| 值函数、Q 函数、off-policy | Taxi、MountainCar | 1 |
| 探索与利用（ε-greedy、熵） | Taxi、阶段 5 | 1、5 |
| 稀疏奖励、信用分配 | MountainCar、DoorKey | 1、5 |
| 连续动作、高斯策略、策略梯度 | Pendulum、Acrobot | 2 |
| Actor-Critic、GAE、PPO 裁剪 | LunarLander、BipedalWalker | 2、3 |
| 部分可观测（POMDP）、记忆 | MiniGrid 系列 | 3、5 |
| 函数逼近、CNN 表征 | CarRacing、Atari | 3、4 |
| 帧堆叠、长程回报 | Atari Pong / Breakout | 4 |
| 内在奖励、奖励塑形 | DoorKey 系列 | 5 |
| 策略坍缩、训练不稳定性 | DoorKey-8x8 | 5 |

### 这个项目**不**覆盖的（学完这里之后的方向）

诚实地说，这只是 RL 的一半。以下都是这个项目里没有的：

- **表格方法**：`taxi` 用的是 DQN，不是 Q 表。经典表格 Q-learning 与值迭代建议单独实现一遍（FrozenLake 十分钟能写完）
- **离线强化学习（Offline RL）**：从固定数据集学，不能交互。见 CQL、IQL、Minari
- **模仿学习**：行为克隆、DAgger
- **多智能体与自博弈**：这个项目里全是单智能体，Atari Pong 的对手是脚本 AI
- **基于模型的 RL**：Dreamer、MuZero 那一系
- **分层 RL**：Options、HRL
- **元学习 / 泛化**：Procgen、Meta-RL
- **逆强化学习**：从演示中恢复奖励函数
- **分布式 RL**：Ape-X、IMPALA（这个项目是单进程的）

---

## 阶段 0：基础设施（CartPole）

**定位**：跑通流程 + 理解 RL 实验的方差有多大。

```bash
python train.py --config cartpole
```

### 概念

CartPole 是最小的一类问题：状态 4 维连续、动作 2 个离散、奖励每步 +1、活到 500 步算成功。

要在这里建立的概念：**回合（episode）**、**回报（return）**、**折扣因子 γ**。CartPole 的 `gamma: 0.99` 意味着 100 步之后的奖励只值现在的 0.37 倍。

### 实验

**实验 0.1 —— 基线**

```bash
python train.py --config cartpole
```

观察 `outputs/<配置名>/logs/progress.csv` 的 `rollout/ep_rew_mean`：它应该在 **10k 步内**从 20 涨到 500。看 `evaluation.json` 的 `mean_reward` 和 `std_reward`。

> 注意 `ep_len_mean` 和 `ep_rew_mean` 在 CartPole 上是**同一个东西**（每步 +1），其他环境不是。养成同时看两个的习惯。

**实验 0.2 —— 种子敏感性（最重要的一个实验）**

改 `experiment.seed` 为 `0`、`1`、`2`，各跑一次，记录收敛步数和最终评估。

**你会看到**：相同的代码、相同的数据分布，只是因为随机种子不同，收敛速度可能差出一倍以上。这是 RL 与监督学习最大的体感差异。

**结论**：从此以后，任何「A 比 B 好」的结论都要有多个种子支撑。单次实验的差异可能完全是噪声。

**实验 0.3 —— 算法切换**

改 `algorithm.name` 为 `A2C`、`DQN`（配置里都提供了 profile），各跑一次。

**分析要点**：
- A2C 的 `n_steps: 5` 对比 PPO 的 `n_steps: 256` —— 每次更新用多少经验？这对梯度方差有什么影响？
- DQN 是 off-policy（用经验回放），PPO/A2C 是 on-policy（用完即弃）。**为什么 CartPole 上 DQN 收敛得可能更慢？** 想想 `learning_starts: 1000` 意味着什么。

**实验 0.4 —— 学习率扫描**

改 `algorithm.profiles.PPO.kwargs.learning_rate` 为 `1e-4`、`3e-4`、`1e-3`。

**分析要点**：画一张「学习率 vs 收敛步数」的图。你应该会看到 U 形——太小则慢，太大则不稳（回报曲线剧烈震荡甚至崩溃）。

### 关键读数

| 文件 | 看什么 |
| --- | --- |
| `logs/progress.csv` | `rollout/ep_rew_mean`（学习曲线）、`train/approx_kl`（更新幅度） |
| `evaluation/evaluations.npz` | 周期性评估，比训练曲线更能反映真实水平 |
| `evaluation.json` | 最终评估的均值和标准差 |
| `trajectories/episode_1.mp4` | 策略的实际行为 |

### 进阶问题

- `approx_kl` 在 PPO 里应该保持在什么范围？超过 `clip_range` 意味着什么？
- 为什么 CartPole 的 `ep_rew_mean` 上限是 500？看 `max_episode_steps`。

---

## 阶段 1：值方法（Taxi、MountainCar）

**定位**：理解值函数，以及稀疏奖励为什么难。

### Taxi —— 值函数的直觉

```bash
python train.py --config taxi
```

**问题设定**：200 个乘客位置 × 4 个目的地 × 5 个出租车位置 × 4 个动作 ≈ 500 个离散状态。接送乘客。

**概念**：**Q 函数** `Q(s,a)` 表示「在状态 s 做动作 a，之后能拿到的总回报」。策略就是 `argmax_a Q(s,a)`。

**实验 1.1 —— 基线**

跑通，观察回报曲线。

**分析要点**：Taxi 的成功奖励是 +20、失败 -10、每步 -1。**负的每步惩罚在教智能体什么？** （提示：尽快完成任务）

**实验 1.2 —— 探索率**

DQN 的探索由 `exploration_fraction`、`exploration_initial_eps`、`exploration_final_eps` 控制：

- 把 `exploration_fraction` 从 `0.2` 改到 `0.05`
- 再改到 `0.5`

**分析要点**：ε 衰减太快 → 还没找到好策略就不再探索；衰减太慢 → 收敛慢。**这是「探索与利用」权衡最直观的一次实验**。

**实验 1.3 —— 为什么要用小状态空间**

Taxi 的状态数只有几百。想象一下：如果状态是连续的呢？值函数就没法用表格存了——**这就是函数逼近登场的动机**，也就是接下来所有环境要用的东西。

### MountainCar —— 稀疏奖励的第一课

```bash
python train.py --config mountaincar
```

**问题设定**：小车马力不足，无法直接爬坡，必须先向反方向倒车积累动能。到达右侧山顶算成功。**每步 -1，直到成功**。

这是你遇到的**第一个真正的稀疏奖励问题**：随机策略几乎永远到不了终点。

**实验 1.4 —— 训练步数扫描（必做）**

改 `training.total_timesteps` 为 `50000`（默认）、`200000`，各跑一次。

**你会看到**：前 2 万步 `ep_rew_mean` 长期贴在 -200（走满 200 步全失败）。然后某一天突然开始上升。

**分析要点**：这叫**「探索悬崖」**——在第一次成功之前，梯度里没有任何关于「成功」的信息，critic 学到的最优预测就是「永远是很低的负值」。**稀疏奖励的所有困难都源于此**。

**实验 1.5 —— 折扣因子 γ（关键实验）**

改 PPO 的 `gamma` 为 `0.9`、`0.99`、`0.999`。

**分析要点**：MountainCar 的最优解需要**先远离目标**（倒车）再靠近。γ 太小（0.9）会让「未来的成功」打折太多，智能体学不会这种迂回策略。**γ 在这里不是调参细节，是决定任务能否学会的关键**。

**实验 1.6 —— 奖励塑形（为阶段 5 铺垫）**

写一个简单的 wrapper，给「接近山顶」额外的奖励（例如按高度给 bonus），再训练一次。

**分析要点**：成功率会大幅提高。但要想清楚代价——**你注入的是领域知识**。智能体学到的是「爬到高处」，如果奖励塑形设计不当（比如按位置给奖励但没考虑方向），它可能学会在高处来回摆动刷分而不去终点。这就是**奖励黑客（reward hacking）**。

这个实验是阶段 5 的预告片。

---

## 阶段 2：连续控制与策略梯度（Pendulum、Acrobot、LunarLander）

**定位**：从"选哪个离散动作"过渡到"输出一个实数向量"。

### Pendulum —— 连续动作入门

```bash
python train.py --config pendulum
```

**问题设定**：一根摆，施加连续力矩让它立起来。状态 3 维、动作 1 维（力矩）。

**概念**：**策略梯度**。动作不再是 `argmax` 出来的，而是从**高斯分布**里采样出来的：网络输出均值 μ 和标准差 σ，动作 `a ~ N(μ, σ)`。训练时让「好动作」的概率上升。

**实验 2.1 —— 基线 + 观察噪声**

跑通后看 `play.py` 生成的轨迹图。

**分析要点**：动作是**连续曲线**而不是 CartPole 那种阶梯状的 0/1。另外注意策略的**随机性**——同一个状态附近，不同回合的动作不完全相同，因为 σ 不为零。

**实验 2.2 —— 熵正则的作用**

改 `ent_coef` 为 `0.0`、`0.01`（默认）两档。

**分析要点**：`ent_coef=0` 时策略会很快变得确定（σ → 0），可能过早收敛到次优的「不动的摆」。这是**熵正则维持探索**的直接演示。

### Acrobot —— 时序信用分配

```bash
python train.py --config acrobot
```

**问题设定**：双连杆摆，下方关节施加离散力矩，把末端甩到指定高度以上。

**概念**：**时序信用分配**。成功那一刻的奖励，要回溯到几十步前「开始往哪个方向荡」的决策上。

**实验 2.3 —— DQN vs PPO 对照**

默认是 DQN。改 `algorithm.name` 为 `PPO` 再跑一次。

**分析要点**：这是**off-policy（DQN）与 on-policy（PPO）的直接对比**。思考：
- DQN 用经验回放，样本效率通常更高（每份数据能反复用）
- 但 DQN 的 `target_update_interval` 引入了目标网络的滞后，训练可能不稳定
- PPO 每次更新后数据就丢弃了，样本效率低但更稳

**在 Acrobot 上哪个赢？** 跑一下，用数据说话。

**实验 2.4 —— 目标网络的作用**

改 DQN 的 `target_update_interval` 为 `1`（等价于去掉目标网络）和 `1000`（默认）。

**分析要点**：`=1` 时目标网络每步都跟着更新，Q 值的目标在追着自己跑，容易发散。这是**「为什么要用目标网络」**的经典演示。

### LunarLander —— 一个环境，两种动作空间

```bash
python train.py --config lunarlander
```

**实验 2.5 —— 离散 vs 连续（必做对照）**

1. 默认配置（离散）跑一次
2. 改 `environment.kwargs` 加 `continuous: true`，同时把 `algorithm.name` 改成 `SAC` 或 `TD3`（配置里提供了 profile），再跑一次

**分析要点**：同一个环境、同一个任务，动作空间表示不同，学习难度差异很大。连续动作空间更大（无限维），但提供了更精细的控制。

**观察**：连续版通常需要更多步才能收敛。记录两者的步数比。

---

## 阶段 3：从状态到像素（MiniGrid、BipedalWalker、CarRacing）

**定位**：观测从「低维数值向量」变成「图像」或「高维物理状态」。

### MiniGrid-Empty-8x8 —— 部分可观测（POMDP）入门

```bash
python train.py --config minigrid
```

**重要的认知纠偏**：MiniGrid 的观测是 **7×7 的局部视野**，不是全局地图。`minigrid_flat` 包装器只移除了文本 mission，**没有**把视图变成全局。也就是说，从第一个 MiniGrid 环境开始，你面对的就是**部分可观测**。

**概念**：**POMDP**。智能体看不到完整状态，理论上需要「信念状态」——对世界的概率估计。实践中常用 RNN 隐状态近似。

**实验 3.1 —— 验证视野是局部的**

改 `environment.kwargs` 加 `agent_view_size: 3`，跑通后看视频。

**分析要点**：视野缩小后智能体明显变笨、撞墙变多。这直接证明观测不是全局的。

**实验 3.2 —— 部分可观测 ≠ 需要记忆**

把 `algorithm.name` 改成 `RecurrentPPO`（配置里提供了 profile），和 MLP 版对比。

**分析要点**：Empty-8x8 房间小（8×8）、视野 7×7，几乎覆盖全场，所以是**近似全观测**——加不加 LSTM 效果差不多。**观测是局部的，不等于任务需要记忆**。这个区分在阶段 5 会变得关键。

### BipedalWalker —— 高维连续控制

```bash
python train.py --config bipedalwalker
```

**问题设定**：24 维状态、4 维连续动作（两个髋关节、两个膝关节的力矩），让双足机器人走路。

**实验 3.3 —— 观察学习过程，而不是只看曲线**

这是本阶段最重要的练习。训练期间会定期保存 checkpoint（`training.checkpoint.save_freq`）。

做法：
1. 训练一个较长的时间（`total_timesteps: 300000`）
2. 训练结束后，把 `visualization.trajectory.model` 改成 `best`，用 `play.py` 回放
3. 对比早期 checkpoint（`checkpoints/` 目录下按步数命名）和最终模型的行为

**分析要点**：只回报曲线的话，你只能看到「0 → 300」。看视频你能看到：**先学会用膝盖撑住不摔倒 → 再学会往前蹭 → 最后学会正常步态**。这是 RL 里最有价值的一种观察，**行为演化的信息量远大于单条曲线**。

### CarRacing —— 像素输入

```bash
python train.py --config car_racing
```

**概念**：**表征学习**。输入是 96×96×3 的图像，策略网络的第一层必须自己学会「什么像素重要」——赛道边缘、车的朝向。这一层是 `CnnPolicy`，不是 `MlpPolicy`。

**实验 3.4 —— 计算开销对比**

对比 `outputs/<配置名>/logs/progress.csv` 的 `fps` 一栏与前面几个环境。

**分析要点**：CNN 前向的开销是实打实的，FPS 会低一个数量级。**这直接决定了你需要在「更聪明的算法」和「更多算力」之间做取舍**。

**实验 3.5 —— 观测降维**

改 `environment.kwargs` 加 `continuous: false`（如果支持），或对比不同分辨率。

**分析要点**：输入维度直接决定学习难度和计算量。

---

## 阶段 4：Atari（Pong、Breakout）

```bash
python train.py --config atari_pong
python train.py --config atari_breakout
```

**定位**：像素输入 + 长程回报。

### 概念

**帧堆叠**：单张画面看不出运动方向（球往哪飞？）。标准做法是把 4 帧堆在一起作为观测。检查一下配置里是否处理了这一点——如果没有，**这就是一个可以改进的实验**。

**长程回报**：Breakout 里打穿一面墙需要连续打几十次砖，回报要很久之后才兑现。

### 实验

**实验 4.1 —— Pong vs Breakout（核心对照）**

两个环境用**完全相同的算法和超参**。观察收敛难度的差异。

**分析要点**：Pong 通常在较少的步数内就能学会接球（赢了立刻有回报，反馈密集）。Breakout 难得多（分数增长慢、回报稀疏）。**同一算法、同样像素输入，为什么差别这么大**——回答这个问题要用「奖励密度」和「信用分配跨度」两个概念。

**实验 4.2 —— 奖励裁剪**

检查配置里有没有对奖励做裁剪（标准做法是把奖励裁到 `[-1, 1]`）。

**分析要点**：Atari 不同游戏的分数量级差异巨大（Pong 21 分 vs Breakout 864 分）。不裁剪的话，同一套超参没法跨游戏通用。

**实验 4.3 —— 训练时就监控**

Atari 训练动辄几小时。用 `monitor-experiment` 或 `training-check` 之类的工具在训练中途检查，**不要等跑完才发现 NaN**。

---

## 阶段 5：稀疏奖励与探索（DoorKey 三级）

**定位**：前四阶段所有概念的综合，也是这个项目里概念密度最高的部分。

三个配置对应三个递进的层次：**能解 → 需要技巧才能解 → 开放问题**。

### 5.1 DoorKey-5x5 —— 稀疏奖励（可解）

```bash
python train.py --config minigrid_doorkey
```

**问题设定**：除了成功那一刻，**每步奖励都是 0**。要找到钥匙 → 捡起 → 开门 → 到达终点。

**关键差异**：对比 MountainCar 的「每步 -1」，DoorKey 是「每步 0」。这意味着**没有失败的成本**，也就没有「尽快完成」的压力——智能体可以一直瞎逛。这是两种不同形态的稀疏奖励。

**实验 5.1 —— 记忆到底需不需要**

改 `algorithm.name` 为 `PPO`（无记忆 MLP）再跑一次。

**实测结果**：PPO 同样成功，而且更快。

**结论**：DoorKey 是**反应式可解**的——「看到钥匙就按 pickup、看到门就按 toggle」这些决策只依赖当前视野，不需要回忆钥匙在哪。所以它不是记忆任务。

**实验 5.2 —— 压缩视野，验证上面的结论**

改 `environment.kwargs` 加 `agent_view_size: 3`（视野压到极小），分别用 PPO 和 RecurrentPPO 跑。

**实测结果**：**两者都达到 100%**。进一步证明 DoorKey 不需要记忆。

> 这两个实验的价值在于：**推翻了一个听起来很合理的直觉**。「迷宫 + 视野受限 = 需要记忆」看起来理所当然，但实验说不是。

### 5.2 DoorKey-6x6 —— 探索（需要技巧）

```bash
python train.py --config minigrid_doorkey_hard
```

**这个配置不带 progress shaping 是解不开的。** 6×6 的钥匙经常落在 7×7 视野之外。

**实验 5.3 —— 对照：删掉 shaping（必做）**

把 `environment.wrappers` 里的 `minigrid_progress` 删掉，其他不动，跑一次。

**实测结果**：

| 配置 | 结果 |
| --- | --- |
| 6x6 无 shaping | 160k 步仍然 **0%** |
| 6x6 + shaping | **20k 步达到 ~100%** |

**亲眼看到从 100% 掉到 0%**，这个冲击比读十篇讲 reward shaping 的文章都强。

**实验 5.4 —— 诊断：到底卡在哪一步**

这是本阶段最重要的实验方法训练。做法：

1. 跑一个**随机策略**，统计：捡到钥匙的比例、开门的比例、到达终点的比例
2. 对照 `docs/minigrid_door_key.md` 里的实测数据

实测结果是：随机策略 **93% 能捡到钥匙**，但只有 **14% 能开门**。

**分析要点**：第一直觉会认为是「找不到钥匙」，但数据说不是。开门需要同时满足「持钥匙 + 站在门旁 + 面向门」——一个精确的 `(位置, 朝向)` 组合。**用数据否定直觉，这是 RL 实验最重要的一项技能。**

**实验 5.5 —— 无效方案的记录：count-based novelty**

我为这个问题实现了 `minigrid_novelty`（把状态离散成 `(x, y, direction)` 计数，给 `scale/sqrt(count)` 的探索奖励）。**结果是失败的**：三种配置跑满 300k 步，成功率全是 0%。

**失败原因**（这个原因比结果本身重要）：**状态 novelty 只奖励「访问新状态」，不奖励「在该状态下按对动作」**。智能体可以无数次站在门旁面朝门，却一直按「前进」而不是 `toggle`。问题是动作层面的，状态计数够不着。

**所以这个包装器我保留在代码里没有删。** 请把它当成一个反例来读：**not every plausible idea works**，而记录失败方案和记录成功方案同样重要。

### 5.3 DoorKey-8x8 —— 策略坍缩（开放问题）

```bash
# 改 config/minigrid_doorkey_hard.yaml：
#   environment.id  → MiniGrid-DoorKey-8x8-v0
#   ent_coef        → 0.05
```

**这是唯一没有稳定解的配置**，也是最好的一个案例研究。

**实验 5.6 —— 三次诊断（完整案例）**

**第一次诊断：提高步数（失败）**

8x8 + shaping 跑到 900k 步，仍然 0%。

**第二次诊断：看训练日志**

shaping 满分是 1.9（捡钥匙 0.5 + 开门 0.5 + 成功 0.9）：

| 环境 | `ep_rew_mean`（末段） |
| --- | --- |
| 6x6（成功） | 1.964 |
| 8x8（900k） | **0.252** |

8x8 的峰值只有 0.578，从未接近 1.0 —— **开门始终没学会**。

**第三次诊断：打印动作分布（关键）**

用训练后的模型跑 100 回合，统计动作分布：

| 动作 | 占比 |
| --- | --- |
| `forward` | 11.1% |
| `pickup` | **37.9%** |
| `drop` | **31.9%** |
| `toggle` | **0.0%** |

而且捡到钥匙的比例从随机策略的 **93% 掉到 11%**。

**结论：这不是探索不足，是策略坍缩。** 智能体收敛到「按 pickup/drop」这类**局部无害动作**（在空地上是空操作，不会撞墙），几乎不前进，也从不按 `toggle`。**PPO 不但没学会，还把策略训得比随机更差。**

**实验 5.7 —— 对症修复：熵正则**

把 `ent_coef` 从 `0.01` 提到 `0.05`：

| 配置 | 训练回报（末段） | 峰值成功率 |
| --- | --- | --- |
| `ent_coef=0.01` | 0.252 | 0% |
| `ent_coef=0.05` | **1.786** | 80% |
| `ent_coef=0.10` | 1.857 | 45% |

训练后策略的 `toggle` 使用率从 **0.0% → 19.4%**，到达终点从 0% → 24%。**因果链闭合**。

**但还是不稳定**：跑到 1M 步，确定性评估成功率仍在 **0% ~ 85%** 之间震荡（最近 6 次均值 46%），训练回报却已达 1.839（上限 1.9）。

**分析要点**：训练回报高说明采样策略会做；确定性执行（`argmax`）波动大，说明策略本身不稳。**`ent_coef=0.05` 维持了较高的动作熵，`argmax` 时容易选偏**——这是熵正则的固有代价。

**未验证的方向**：熵系数退火（先高后低）、从 6x6 预训练迁移的课程学习、惩罚重复空操作。

### 阶段 5 的三条方法论教训

**1. 先看行为分布，再猜原因。**
「8x8 学不会」的第一直觉是探索不足，据此上 novelty 白跑了 **120 万步**。而动作分布一眼就显示根因是策略坍缩。**诊断顺序反了的代价是实打实的算力。**

**2. 稀疏奖励下，熵正则不是可有可无的装饰。**
`ent_coef=0.01` 在 6x6 上够用，在 8x8 上直接导致坍缩。**同一套超参跨环境尺寸并不成立。**

**3. critic 塌缩的识别特征。**
当绝大多数回报都是 0，价值函数的最优解就是恒定预测 0。看到 `value_loss` 趋近于 0（如 `2e-08`）且 `explained_variance` 接近 0 的组合，基本可以断定 critic 没学到东西。

---

## 通用实验方法

### 怎么读训练曲线

| 现象 | 可能原因 | 排查 |
| --- | --- | --- |
| 回报完全不涨 | 学习率过大/过小、奖励尺度问题 | 看 `train/approx_kl` 是否异常 |
| 回报先涨后崩 | 学习率过大、灾难性遗忘 | 降低学习率，看 `entropy_loss` |
| 回报震荡剧烈 | 随机性大 / 批量太小 | 多跑几个种子确认不是噪声 |
| `value_loss` → 0 且回报不涨 | **critic 塌缩**（稀疏奖励） | 见阶段 5 |
| `ep_len_mean` 一直打满 | 从未成功，也从未提前终止 | 打印动作分布诊断 |

### 对照实验的设计原则

1. **一次只改一个变量**。同时改两个，结果无法归因
2. **固定种子集合**。比较 A 和 B 时用同样的种子
3. **跑够步数**。很多「失败」只是还没到时候（MountainCar 前 20k 步）
4. **看评估曲线而非训练曲线**。训练曲线受探索噪声影响；周期性评估（`evaluation.npz`）更可靠
5. **关键结论至少 3 个种子**

### 失败诊断流程

按这个顺序排查，不要跳步：

```
回报不涨
  ├─ 看 fps：是不是慢到还没训够？ → 是则加步数
  ├─ 看 ep_len_mean：一直打满？ → 是则从未成功过
  ├─ 打印动作分布：某几个动作占绝对主导？ → 是则策略坍缩，调 ent_coef
  ├─ 看 value_loss / explained_variance：都接近 0？ → 是则 critic 塌缩
  └─ 看 approx_kl：异常大？ → 是则学习率过大
```

**核心原则：用数据否定直觉，而不是用直觉解释数据。**

---

## 常见坑

**别把「部分可观测」和「需要记忆」划等号。** 观测是局部的（POMDP）不等于策略必须记忆——如果任务反应式可解，无记忆策略照样能行。DoorKey-5x5 是实测反例。

**奖励塑形是把双刃剑。** 它能解决稀疏奖励，但你注入的是领域知识，设计不当会导致 reward hacking（智能体刷分而不完成任务）。

**熵正则按环境调，没有通用值。** `ent_coef` 太低会坍缩，太高则策略不稳（阶段 5 的 8x8）。

**不要把训练慢当成训练失败。** 先看 `outputs/<配置名>/logs/progress.csv` 的 `fps`，再判断是否需要更多步数。

**本机小网络用单线程更快。** 配置里的 `experiment.torch_threads: 1` 是有实验依据的：256 核机器上小 LSTM 策略单线程比默认快约一倍（默认 128 线程 224 FPS，单线程 462 FPS）。但**像素环境用的是 CNN，这条不适用**——不要无脑套用。

**记录失败的实验。** 阶段 5.5 的 novelty 是个好例子：方案失败了，但「为什么失败」的结论比成功案例更有价值。
